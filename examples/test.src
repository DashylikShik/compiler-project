fn factorial(int n) -> int {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}

fn main() -> void {
    int x = 5;
    int result = 0;
    
    result = factorial(x);
    
    while (x > 0) {
        x = x - 1;
    }
}