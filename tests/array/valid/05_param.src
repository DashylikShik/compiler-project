fn sum(int arr[], int size) -> int {
    int total = 0;
    for (int i = 0; i < size; i = i + 1) {
        total = total + arr[i];  // arr[i] = *(arr + i)
    }
    return total;
}

fn main() -> int {
    int arr[5] = {1,2,3,4,5};
    return sum(arr, 5);
}